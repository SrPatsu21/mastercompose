services:
  gitlab:
    image: gitlab/gitlab-ce:latest
    restart: unless-stopped
    environment:
      GITLAB_OMNIBUS_CONFIG: |
        external_url 'https://gitlab.com'
        nginx['listen_port'] = 80
        nginx['listen_https'] = false  # Nginx reverse proxy handles HTTPS
    volumes:
      - ./core/gitlab/config:/etc/gitlab
      - ./core/gitlab/logs:/var/log/gitlab
      - ./core/gitlab/data:/var/opt/gitlab
    networks:
      - internal


  # portus:
  #   image: opensuse/portus:head
  #   restart: unless-stopped
  #   environment:
  #     PORTUS_MACHINE_FQDN_VALUE: portus.local
  #   volumes:
  #     - ./portus:/data
  #   networks:
  #     - internal

  # backrest:
  #   image: ghcr.io/pgbackrest/backrest:latest
  #   restart: unless-stopped
  #   volumes:
  #     - ./backups:/backups
  #     - ./gitlab/data:/source/gitlab:ro
  #     - ./portus:/source/portus:ro
  #   networks:
  #     - internal

networks:
  internal:
    external: true
